use walkdir::DirEntry;

pub fn is_top_level_node_modules(entry: &DirEntry) -> bool {
    entry
        .file_name()
        .to_str()
        .map(|s| s.matches("node_modules").count() == 1)
        .unwrap_or(false)
}
